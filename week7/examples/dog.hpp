// dog.h header file
// contains just prototypes and definitions.

// the class methods(fumctions) can be included in a dog.cpp file.
// for this example, they are in class2.cpp.

class dog {
    public:
    dog(int times = 1);
    ~dog();
};
